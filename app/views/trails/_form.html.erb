<%= form_for(@trail, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @trail.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@trail.errors.count, "error") %> prohibited this trail from being saved:</h4>

      <ul>
      <% @trail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :campsite, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :campsite, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :water_source, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.check_box :water_source, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
